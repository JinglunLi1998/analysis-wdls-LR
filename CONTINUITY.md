# CONTINUITY

## [PLANS]

- 2026-09-16T00:00Z [USER] Add a standalone Long-read pVACnc WDL test module before integrating it into `immuno_longread.wdl`.
- 2026-09-17T00:00Z [USER] Integrate pVACnc into the publishable Long-read immuno WDL; do not expose a case-specific phased-VCF `.tbi` workaround as a production input.

## [DECISIONS]

- 2026-09-16T00:00Z [USER] Reuse `jinglunli/pvacnc:0.2.3` for the first Long-read WDL test; do not create a new Docker image until a Long-read-specific container need is demonstrated.
- 2026-09-16T00:00Z [CODE] Preserve the existing Long-read canonical pVACseq command/settings. Extract only its RNA readcount plus StringTie GX/TX VCF annotation steps as a reusable WDL subworkflow.

## [PROGRESS]

- 2026-09-16T00:00Z [TOOL] Confirmed this branch contains PacBio HiFi Long-read DNA/RNA/immuno WDLs, including pbmm2, DeepSomatic, Clair3, HiPhase, minimap2 and StringTie3.
- 2026-09-16T00:00Z [CODE] Added `pvacseq_longread_annotation.wdl`, preserving the existing Long-read RNA readcount and StringTie GX/TX annotation calls as a shared indexed-VCF boundary. Refactored canonical `pvacseq_longread.wdl` to consume that boundary without changing its pVACseq task inputs.
- 2026-09-16T00:00Z [CODE] Added standalone `pvacnc_longread.wdl` plus ORFanage, TransDecoder, preparation and pVACview tool WDLs. The module runs with-`e` and no-`e` ORF lanes from existing Long-read outputs and uses `jinglunli/pvacnc:0.2.3`; `immuno_longread.wdl` remains unchanged.
- 2026-09-16T00:00Z [CODE] Updating the Long-read branch from `analysis-wdls/main` changed the shared pVACtools task from 6.0.5 to `griffithlab/pvactools:7.1.2`. Updated Long-read pVACseq, pVACsplice, pVACfuse and pVACnc callers to the 7.1.2 interface: three optional percentile threshold inputs and `Array[String]? top_score_metric2`. No threshold values were assigned; pVACtools defaults apply unless supplied by a workflow input.
- 2026-09-16T00:00Z [CODE] Added `example_data/pvacnc_longread_ntr004combined.yml` for a standalone module-boundary test using completed NTR004combined LR outputs. The test supplies all three pVACtools 7.1.2 percentile thresholds as `1`, matching the established short-read 7.1.2 configuration, while retaining the prior LR coverage/VAF thresholds.
- 2026-09-17T00:00Z [TOOL] First standalone run `44ea1aff-6dbf-441f-b2ce-ddfa1f2cb7dd` completed StringTie3, both ORFanage jobs, ORFanage preparation, and both TransDecoder predictions, then failed in ORFanage pVACseq because the exported phased proximal VCF had no adjacent `.tbi`.
- 2026-09-17T00:00Z [CODE] Updated the standalone test to use a case-local phased VCF/TBI pair, matching the old LR preparation logic. Added a test-specific persistent call-caching Cromwell configuration with nested LSF job group `/jinglun/wdl`; the first run used an in-memory database and therefore cannot seed the new cache.
- 2026-09-17T00:00Z [CODE] Integrated `pvacncLongread` into `immunoLongread` behind `enable_pvacnc=false`. The production call receives the phased proximal VCF and TBI directly from `somaticPacbioHifi`, reuses the canonical pVACseq expression-annotated VCF, and publishes separate ORFanage and TransDecoder output arrays. Added the phased proximal TBI to the top-level workflow outputs.
- 2026-09-17T00:00Z [TOOL] MiniWDL 1.15.0 successfully type-checked the complete `immuno_longread.wdl` import graph and the standalone `pvacnc_longread.wdl`; only pre-existing lint warnings were reported.
- 2026-09-17T09:19Z [TOOL] NTR004combined LR run `73d9c5b7-f74f-4862-989f-f387963082bb` completed StringTie3, both ORFanage calls, ORFanage preparation/pVACseq, and both TransDecoder predictions, then failed because pVACview annotation treated zero `no_e_rescue` transcripts as fatal.
- 2026-09-17T09:30Z [CODE] pVACnc `0.2.4` changes only the shared pVACview annotation edge case: zero `no_e_rescue` transcripts is now a valid no-op for either lane; malformed mappings and incomplete bundles remain fatal. Updated LR WDL defaults and the standalone test input to `jinglunli/pvacnc:0.2.4`.
- 2026-09-17T19:00Z [TOOL] The `0.2.4` rerun progressed through ORFanage pVACseq and both TransDecoder predictions, then TransDecoder preparation failed because `NTR004combined_tumor.record_013914.ALT1 / ENST00000456049.p5` was provisionally protein-changing but reconstructed identical wild-type and mutant proteins.
- 2026-09-17T19:05Z [USER] Allele--ORF pairs with identical reconstructed wild-type and mutant proteins must be dropped rather than terminating the complete pipeline.
- 2026-09-17T19:05Z [CODE] pVACnc `0.2.5` skips unchanged translated TransDecoder allele--ORF pairs, reports their count and identities in the preparation log, and retains their upstream effects TSV rows for audit. Updated LR WDL defaults and the standalone input to `jinglunli/pvacnc:0.2.5`.
- 2026-09-17T19:30Z [USER] Package all legitimate zero-candidate handling together in pVACnc `0.2.5` so an empty biological result does not terminate the complete workflow.
- 2026-09-17T19:30Z [CODE] ORFanage and TransDecoder filtering, routing and synthetic-VCF construction now emit header-only artifacts for valid empty results. Each preparation task writes a candidate count; `pvacncLongread` conditionally runs pVACseq/pVACview only when that lane has candidates and otherwise publishes empty prediction arrays. Input/schema/reference inconsistencies remain fatal.
- 2026-09-17T19:30Z [TOOL] Empty reference-CDS filtering, empty ORFanage routing/builder, empty TransDecoder routing/builder, unchanged-protein skipping, Python compilation and shell syntax checks passed. Local MiniWDL execution was unavailable because the mounted launcher and interpreter are cluster Linux executables; cluster-side type checking remains required after rebuilding `workflows.zip`.
- 2026-09-17T20:00Z [USER] Maintain separate version trackers for Docker image changes and WDL changes.
- 2026-09-17T20:00Z [CODE] Added the container-level `CHANGELOG.md` for image versions `0.1.0` through `0.2.5` and `PVACNC_WDL_CHANGELOG.md` for Long-read workflow commits/contracts. The READMEs cross-reference the separate trackers; incompletely evidenced early image history is marked `UNCONFIRMED`.
- 2026-09-17T20:55Z [CODE] Added `one_time_recovery_ntr004_transdecoder_025/`, a deliberately case-specific recovery workflow for failed run `b1d4d503-3114-4222-832f-c00adf8eb2eb`. It reuses completed no-`-e` StringTie and with-`-e`/no-`-e` TransDecoder artifacts and reruns only TransDecoder preparation, conditional pVACseq and pVACview annotation with image `jinglunli/pvacnc:0.2.5`. The submit script checks every exact retained input before submission and writes to `transdecoder_recovery_outputs_025`; it is not part of the publishable workflow.
- 2026-09-21T00:00Z [CODE] The NTR004 recovery completed preparation and TransDecoder pVACseq, but `transdecoderAnnotatePvacview` was killed with exit 137 under its 4-GB request. Added an annotation-only one-time recovery that copies the retained pVACseq outputs to `transdecoder_annotation_only_025`, requests 64 GB and runs only `pvacnc-annotate-pvacview`; it refuses to overwrite an existing destination.
- 2026-09-21T18:30Z [CODE] Permanently increased `pvacncAnnotatePvacview` memory from 4 GB to 16 GB after NTR004 measured 5.75 GB and failed under the previous limit. This changes resources only, not candidate semantics. Rebuilt `workflows.zip`.
